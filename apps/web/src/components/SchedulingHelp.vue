<template>
  <details class="mt-4 text-xs leading-5 text-slate-500 dark:text-slate-400">
    <summary class="cursor-pointer">How tasks, events, and Google sync work</summary>
    <div class="mt-2 max-w-2xl space-y-2">
      <p>
        Tasks are work you can complete. Events are separate calendar entries, not checkable tasks.
      </p>
      <p>
        With Google connected, scheduled tasks mirror to your primary Google calendar when it is
        selected and writable. Unscheduled tasks do not sync. Select your primary calendar in
        Calendars; reconnect Google in Settings if permissions need attention.
      </p>
      <p>
        Completing a task keeps its time block. Unscheduling or deleting a task removes its Google
        block. Deleting that block in Google unschedules the task without deleting it.
      </p>
    </div>
  </details>
</template>
